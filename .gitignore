node_modules/
dist/
*.log
.DS_Store
.remember/
# mif-validate hydrates the canonical schema into this cache; never the authority.
schema/.cache/
# mif-docs ontology hydrated from ../ontologies repo; pulled into the build at release.
ontologies/.cache/
# local semantic vector store (mif-corpus skill); derived, never committed.
.mif/
