---
diataxis_type: reference
title: A Non-MIF Document
description: Lightweight frontmatter with a genre signal but no MIF L1 fields; the fail-closed guard must block this.
---

# A Non-MIF Document

This fixture carries a `diataxis_type` genre signal but lacks the MIF L1 floor
(`id`, `type`, `created`). The guard must block it.
